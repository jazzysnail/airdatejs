'use strict';

((win) => {
  const config = {
    theme  : 'default',
    format : 'YYYY-MM-DD',
    min: '',
    max: ''
  }


  win.airDate = function ({
    theme = 'default'
  } = {}) {
    return theme
  }




})(window)
